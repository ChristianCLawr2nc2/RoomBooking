<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Registro - Room Booking</title>
  <link rel="stylesheet" href="/css/registro/style.css">
</head>
<body>
  <header>
    <img src="/assets/logo.png" alt="Logo do Room Booking">
  </header>
  
  <main>
    <div class="main-section">
      <div class="register-card">
        <h2 class="register-title">Criar Conta</h2>

        <% if (error) { %>
          <p class="error-message"><%= error %></p>
        <% } %>

        <form method="POST" action="/registro" class="register-form">
          <div class="input-group">
            <label for="nome" class="input-label">Nome Completo</label>
            <input type="text" id="nome" name="nome" class="input-field" required>
            <label for="email" class="input-label">E-mail</label>
            <input type="email" id="email" name="email" class="input-field" required>
            <label for="senha" class="input-label">Senha</label>
            <input type="password" id="senha" name="senha" class="input-field" required minlength="6">
          </div>

          <button type="submit" class="register-button">Criar Conta</button>
        </form>

        <div class="voltar-login">
          <a href="/login">Já tem conta? Faça login</a>
        </div>
      </div>
    </div>
  </main>
</body>
</html>